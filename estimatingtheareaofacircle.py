

#! /user/bin/python3

import sys, math

def main():
    per = []
    for line in sys.stdin:
        line = line.split()
        code = [str(n) for n in line]
        per.append(code)
    for x in range(0, len(per), 2):
        if per[0][x]

        

    
    
    
if __name__ == "__main__":
    main()
